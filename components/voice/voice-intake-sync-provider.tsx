"use client";

/** Twilio-Intake-Sync deaktiviert — v1 nutzt MockProvider clientseitig. */
export function VoiceIntakeSyncProvider({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}
