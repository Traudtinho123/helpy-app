"use client";

import { VoiceAssistantPanel } from "@/features/voice/components/voice-assistant-panel";

/** Voice-Einstellungen (Begrüßung, KI-Hinweis, Aktivierung). */
export function VoiceSettingsPanel() {
  return <VoiceAssistantPanel />;
}
